{
  "name": "conversational-assessment-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run build -w @cap/shared && concurrently \"npm run dev -w packages/frontend\" \"npm run dev -w packages/backend\"",
    "build": "npm run build -w @cap/shared && npm run build -w packages/frontend && npm run build -w packages/backend",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
