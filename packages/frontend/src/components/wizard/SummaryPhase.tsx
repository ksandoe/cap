/**
 * SummaryPhase.tsx — TODO: implement
 */
export function SummaryPhase({ children, currentPhase }: any) {
  return (
    <div style={{ padding: 32, fontFamily: 'sans-serif', maxWidth: 720, margin: '0 auto' }}>
      <p style={{ color: '#888' }}>SummaryPhase — not yet implemented</p>
      {children}
    </div>
  );
}
