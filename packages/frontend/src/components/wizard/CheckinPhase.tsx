/**
 * CheckinPhase.tsx — TODO: implement
 */
export function CheckinPhase({ children, currentPhase }: any) {
  return (
    <div style={{ padding: 32, fontFamily: 'sans-serif', maxWidth: 720, margin: '0 auto' }}>
      <p style={{ color: '#888' }}>CheckinPhase — not yet implemented</p>
      {children}
    </div>
  );
}
