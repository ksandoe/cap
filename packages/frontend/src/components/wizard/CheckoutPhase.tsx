/**
 * CheckoutPhase.tsx — TODO: implement
 */
export function CheckoutPhase({ children, currentPhase }: any) {
  return (
    <div style={{ padding: 32, fontFamily: 'sans-serif', maxWidth: 720, margin: '0 auto' }}>
      <p style={{ color: '#888' }}>CheckoutPhase — not yet implemented</p>
      {children}
    </div>
  );
}
